import math;


abs()	# Returns absolute value of a number
divmod() #	Returns quotient and remainder of integer division
max()	# Returns the largest of the given arguments or items in an iterable
min()	# Returns the smallest of the given arguments or items in an iterable
pow()	# Raises a number to a power
round()	# Rounds a floating-point value
sum()	# Sums the items of an iterable



ascii()	# Returns a string containing a printable representation of an object
bin()	# Converts an integer to a binary string
bool()	# Converts an argument to a Boolean value
chr()	# Returns string representation of character given by integer argument
complex()	# Returns a complex number constructed from arguments
float()	# Returns a floating-point object constructed from a number or string
hex()	# Converts an integer to a hexadecimal string
int()	# Returns an integer object constructed from a number or string
oct()	# Converts an integer to an octal string
ord()	# Returns integer representation of a character
repr()	# Returns a string containing a printable representation of an object
str()	# Returns a string version of an object
type()	# Returns the type of an object or creates a new type object


all()	# Returns True if all elements of an iterable are true
any()	# Returns True if any elements of an iterable are true
enumerate()	# Returns a list of tuples containing indices and values from an iterable
filter()	# Filters elements from an iterable
iter()	# Returns an iterator object
len()	# Returns the length of an object
map()	# Applies a function to every item of an iterable
next()	# Retrieves the next item from an iterator
range()	# Generates a range of integer values
reversed()	# Returns a reverse iterator
slice()	# Returns a slice object
sorted()	# Returns a sorted list from an iterable
zip()	#Creates an iterator that aggregates elements from iterables


# The Python interpreter supports many functions that are built-in: sixty-eight,
# as of Python 3.6. You will cover many of these in the following discussions,
# as they come up in context.

# For now, a brief overview follows, just to give a feel for what is available.
# See the Python documentation on built-in functions for more detail. 
# Many of the following descriptions refer to topics and concepts that will be discussed in future tutorials.


callable()	# Returns True if object appears callable
compile()	# Compiles source into a code or AST object
eval()	# Evaluates a Python expression
exec()  # Implements dynamic execution of Python code
hash()	# Returns the hash value of an object
help()	# Invokes the built-in help system
memoryview()	# Returns a memory view object
staticmethod()	# Returns a static method for a function
__import__()	# Invoked by the import statement


# Variables, References, and Scope
# Function	Description
dir()	#Returns a list of names in current local scope or a list of object attributes
globals()	#Returns a dictionary representing the current global symbol table
id()	#Returns the identity of an object
locals()	#Updates and returns a dictionary representing current local symbol table
vars()	# Returns __dict__ attribute for a module, class, or object